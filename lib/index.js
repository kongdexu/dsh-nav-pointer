// dsh-nav-pointer — host half (no-op; this plugin is client-only).
export const name = 'dsh-nav-pointer';
export const inject = [];
export function apply(_ctx, _config) {
  // All functionality lives in the client half (lib/client.js).
}
