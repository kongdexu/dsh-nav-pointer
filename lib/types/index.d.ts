/** dsh-message-pointer — host half (no-op; plugin is client-only). */
export declare const name: 'dsh-message-pointer';
export declare const inject: readonly [];
export declare function apply(ctx: unknown, config?: unknown): void;
